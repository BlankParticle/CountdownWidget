<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Debug builds only: lets adb simulate elapsed countdown time -->
        <receiver
            android:name=".DebugStateReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.blankparticle.countdown.DEBUG_SET_ELAPSED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
